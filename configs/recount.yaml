run_dir: data/artifacts/StartTO_17Jan_crops

# Секунды от начала видео до старта гонки (lap 1 считается с этого момента).
# По умолчанию 0 — первый круг от начала видео.
# race_start_sec: 0.0

# Альтернатива: задать время суток старта в ISO-формате.
# Требует run_info.json в папке прогона (создаётся автоматически с версии ...).
# race_start_at: "10:31:00"
